<div class="tags">
  <div class="tag location"><%= meeting.location %></div>
  <% unless meeting.costs_money %>
    <div class="tag free">Gratis</div>
  <% end %>
  <div class="tag organizer"><%= meeting.organizer %></div>
</div>
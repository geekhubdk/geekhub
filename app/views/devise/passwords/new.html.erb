<h2>Glemt dit kodeord?</h2>
<p>Udfyld formularen nedenfor. Du vil modtage en mail med et link du skal trykke på.</p>
<%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>
  <%= devise_error_messages! %>

  <%= f.label :email %>
  <%= f.email_field :email %>

  <div class="form-actions"><%= f.submit "Send", class: "btn btn-primary" %></div>
<% end %>

<%= render "devise/shared/links" %>


<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <legend>Log ind</legend>
  <p>Du skal være logget ind for at indsende events. Tryk på <i>opret bruger</i> for at oprette en profil.<br/>Denne profil giver dig mulighed for at senere at kunne opdatere dit event, hvis der skulle komme ændringer.</p>
  <%= f.label :email, class: "control-label" %>
  <%= f.email_field :email %>

  <%= f.label :password, class: "control-label" %>
  <%= f.password_field :password %>

  <% if devise_mapping.rememberable? -%>
    <label class="checkbox">
      <%= f.check_box :remember_me %> Husk mig
    </label>
  <% end %>

  <div class="form-actions">
    <%= f.submit "Log ind", class: "btn btn-large btn-primary" %>
    <%= link_to "Opret bruger", new_registration_path(resource_name), class: "btn btn-small" %>
  </div>

<% end %>

<%= render "devise/shared/links" %>
